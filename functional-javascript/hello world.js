function upperCaser(input){ // Note here the variable name(function upperCaser is 
//) is lowercase. All variables must start with lowercase and cannot include
//special characters?
	transform = input.toUpperCase();
	return transform;
}

module.exports = upperCaser;